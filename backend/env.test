# Test Environment Variables
PORT=3001
NODE_ENV=test
APP_BASE_URL=http://localhost:3001

# Test PostgreSQL Database (use in-memory or separate test DB)
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_USER=nest_test
POSTGRES_PASSWORD=nestpass_test
POSTGRES_DB=nestdb_test
DATABASE_URL=postgres://nest_test:nestpass_test@localhost:5432/nestdb_test

# Mock SendGrid for tests (no real emails sent)
SENDGRID_API_KEY=test_key
SENDGRID_VERIFICATION_TEMPLATE_ID=d-test-verification-template
SENDGRID_WELCOME_TEMPLATE_ID=d-test-welcome-template
MAIL_FROM_NAME=Test Email Verification
MAIL_FROM_EMAIL=test@example.com

# Test JWT Secret
JWT_SECRET=test_jwt_secret_key
